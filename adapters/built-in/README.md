# Built-in adapters

Built-in adapters are reviewed with the application source and may only map
known products to operations implemented by the trusted Rust core. Version
0.1.0 does not ship a product-specific built-in adapter; generic schema
examples live under `examples/adapter-packs` and are never loaded as real
machine discoveries.
