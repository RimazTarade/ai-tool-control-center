# Community adapters

Community adapters are data only. They may select typed core detections and
operations from `../schema/adapter.schema.json`; they may not contain scripts,
executables, shell text, raw arguments, administrator commands, or arbitrary
network destinations. Contributions need generic fixtures and documentation.
