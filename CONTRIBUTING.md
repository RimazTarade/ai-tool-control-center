# Contributing

Open an issue before large changes. Keep adapters declarative, use generic
fixtures, and never commit local inventories, credentials, logs, databases, or
absolute user paths. Run `python scripts/repository_policy.py .`, Rust tests,
Python checks, and frontend checks before opening a pull request.
