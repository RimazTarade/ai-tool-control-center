# Adapter authoring

Adapters select typed detections and operations from
`adapters/schema/adapter.schema.json`. They cannot run code. Use generic
fixtures, HTTPS documentation, stable lowercase IDs, and semantic versions.
