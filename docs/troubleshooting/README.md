# Troubleshooting

If a source build fails, confirm the pinned Rust, Node, pnpm, and Python
versions first. Share only redacted errors. Never upload local databases,
catalogues, logs, environment variables, or configuration files.
