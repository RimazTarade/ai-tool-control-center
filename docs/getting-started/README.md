# Getting started from source

Install the pinned development toolchains, run `pnpm install --frozen-lockfile`,
then `cargo test --workspace --locked`, `pnpm test`, and `pnpm tauri dev`.
Version 0.1.0 has no end-user binary release.
